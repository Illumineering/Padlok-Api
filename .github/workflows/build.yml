name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Ubuntu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build debug
        run: swift build -v
      - name: Build release
        run: swift build -c release -v
